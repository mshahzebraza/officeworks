import router, { useRouter } from 'next/router';

const ModulePage = () => {
    // router.isReady && router.replace('/404')
    return 'throws the Error during prod. build: You should only use router in the client side of your app'
};

export default ModulePage;